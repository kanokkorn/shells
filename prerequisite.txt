Prerequisites
=============

essential packages after fresh install system

Debian base
----------
sudo apt install tmux git vim curl clang

Arch base
---------
sudo pacman -S tmux git vim curl clang

FreeBSD
-------
sudo pkg install tmux git vim curl
